let number; 

// 예측 가능하고 디버깅 하기 쉬운 코드를 사용하자
// 연산자 우선 순위를 외우는 것은 () 괄호 사용으로 대체하자

function increment(number) {
    // number --
    number = number - 1
}

function decrement(number) {
    // number ++
    number = number + 1 
}

// if((isLogin && token) || user)